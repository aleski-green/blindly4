name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Toolchain
        run: swift --version
      - name: Build
        run: swift build
      - name: Dependency-free self-test
        run: swift run blindly4 --self-test
      - name: Machine-readable schema
        run: swift run blindly4 schema
      - name: Package test binary
        uses: actions/upload-artifact@v4
        with:
          name: blindly4-${{ runner.arch }}
          path: .build/debug/blindly4
